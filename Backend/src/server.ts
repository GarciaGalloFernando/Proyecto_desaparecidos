import { createApp } from "./app.js";import { connectDatabase } from "./config/database.js";import { env } from "./config/env.js"; async function start(){await connectDatabase();createApp().listen(env.port,env.host,()=>console.log(`API disponible en http://${env.host}:${env.port}`));} start().catch(error=>{console.error("No fue posible conectar con MongoDB; el servidor no se iniciará.",error);process.exit(1);});

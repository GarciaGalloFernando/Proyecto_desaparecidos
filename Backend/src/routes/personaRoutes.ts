import { Router } from "express"; import * as c from "../controllers/personaController.js"; import { requireAdmin,requireAuth } from "../middlewares/authMiddleware.js"; import { asyncHandler } from "../utils/asyncHandler.js"; const r=Router();r.get("/",asyncHandler(c.list));r.get("/:id",asyncHandler(c.get));r.post("/",requireAuth,requireAdmin,asyncHandler(c.create));r.patch("/:id",requireAuth,requireAdmin,asyncHandler(c.update));r.delete("/:id",requireAuth,requireAdmin,asyncHandler(c.remove));export default r;

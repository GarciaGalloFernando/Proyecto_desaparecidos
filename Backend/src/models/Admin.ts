import { Schema, model, type InferSchemaType } from "mongoose";
const schema=new Schema({nombre:{type:String,required:true,trim:true},carnet:{type:String,required:true,trim:true,unique:true},email:{type:String,required:true,trim:true,lowercase:true,unique:true},passwordHash:{type:String,required:true,select:false},rol:{type:String,enum:["ADMIN","SUPER_ADMIN"],default:"ADMIN"},estado:{type:String,enum:["ACTIVO","INACTIVO"],default:"ACTIVO"},fecha_creacion:{type:Date,default:Date.now},fecha_actualizacion:{type:Date,default:Date.now}}, {collection:"admins",versionKey:false}); schema.pre("save",function(next){this.fecha_actualizacion=new Date();next();}); export type Admin=InferSchemaType<typeof schema>; export const AdminModel=model("Admin",schema);

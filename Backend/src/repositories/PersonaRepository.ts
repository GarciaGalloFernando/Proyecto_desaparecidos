import { type FilterQuery } from "mongoose"; import { PersonaModel, type Persona } from "../models/Persona.js";
export type PersonaFilters={nombre?:string;departamento?:string;edad?:string;edadMin?:string;edadMax?:string;genero?:string;estado?:string};
export const personaRepository={ async findAll(filters:PersonaFilters){const q:FilterQuery<Persona>={}; if(filters.nombre)q.nombre={$regex:filters.nombre,$options:"i"}; for(const key of ["departamento","genero","estado"] as const) if(filters[key])q[key]=filters[key]; const age:Record<string,number>={}; if(filters.edad) age.$eq=Number(filters.edad); if(filters.edadMin)age.$gte=Number(filters.edadMin); if(filters.edadMax)age.$lte=Number(filters.edadMax); if(Object.keys(age).length)q.edad=age; return PersonaModel.find(q).sort({fecha_registro:-1}).lean();}, findById:(id:string)=>PersonaModel.findById(id).lean(), create:(data:Partial<Persona>)=>PersonaModel.create(data), update:(id:string,data:Partial<Persona>)=>PersonaModel.findByIdAndUpdate(id,{$set:{...data,fecha_actualizacion:new Date()}},{new:true,runValidators:true}).lean(), remove:(id:string)=>PersonaModel.findByIdAndDelete(id).lean() };

import type { ErrorRequestHandler } from "express"; export const errorMiddleware:ErrorRequestHandler=(err,req,res,next)=>{void req;void next; if(err.name==="CastError")return res.status(400).json({message:"El identificador proporcionado no es válido."}); if(err.name==="ValidationError")return res.status(400).json({message:err.message}); if(err.name==="UnauthorizedError")return res.status(401).json({message:"Token no válido o expirado."}); console.error(err);return res.status(500).json({message:"Ocurrió un error interno."});};
